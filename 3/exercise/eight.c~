#include <stdio.h>
int main(void)
{
    float cup;
    
    scanf("%f", &cup);
    printf("%f cup = %f pint = %f ounce = %f tablespoon = %f teaspoon\n", cup, cup / 2,
           cup / 8, cup / 8 / 2, cup /8 / 2 * 3);

    return 0;
}
