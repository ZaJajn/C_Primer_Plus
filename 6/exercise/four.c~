#include <stdio.h>

int main(void)
{
    int row, i, j;
    char ch[10];

    scanf("%d", &row);
    for (i = 0, ch[i] = 'A'; i < row; i++)
    {
        for (j = 0; j <= i; ch[j]+= j, printf("%c", ch[j]), j++);
        printf("\n");
    }

    return 0;
}
