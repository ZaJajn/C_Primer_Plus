#include <stdio.h>
#define SIZE 8
int main(void)
{
    int num[SIZE];
    int i;

    for (i = 0; i < SIZE; i++)
    {
        scanf("%d", &num[i]);
        printf("%d ", num[SIZE - 1 - i]);
    }
    printf("\n");

    return 0;
}
