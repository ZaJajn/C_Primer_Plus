#include <stdio.h>
#include <string.h>

int main(void)
{
    char ch[40];
    int i;
    
    for(i = 0; scanf("%c", ch[i]) == 1; i++);
    i = strlen(ch);
    ch[i] = '\0';
    for(i = strlen(ch) - 1; i >= 0; i--)
        printf("%c", ch[i]);
    return 0;
}
